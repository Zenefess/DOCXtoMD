An insertion is kept.

Only this survives.

Moved to here.

# Inside a block content control

Inside an inline content control.

A smart tag is transparent. So is custom XML.

A link to [somewhere](https://example.invalid/somewhere) keeps its text.

7 is a cached field result.

A complex field result.

The fallback is what is taken.

With no fallback the choice is kept.

The last paragraph.
