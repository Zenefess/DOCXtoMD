A `printf` call.

Use `x = 1;` here.

Also `LibreOffice`.

Ticks ``a`b``.

Edge `` `x ``.

Raw `*not* _emphasis_ & <tag>`.

`run` after

Merged `code`.

`bold code`

<del>`struck code`</del>

A paragraph style called Source Text is not code.

```
int main() {
   return 0;
}
```

Between the fences.

````
first

second ``` third
after a break
````

After.

A whitespace run between two monospace ones does not break the fence:

```
let a = 1;
```

A backtick run split across two spans still sizes the fence:

````
```x
````

A fence does not open on a line of padding, and keeps a break inside it:

```
one

```

A blank line between two samples leaves them one fence:

```
first sample
second sample
```

Bold code merges with plain code: `code`.

```
int x;
```
