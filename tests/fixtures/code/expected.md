A `printf` call.

Use `x = 1;` here.

Also `LibreOffice`.

Ticks ``a`b``.

Edge `` `x ``.

Raw `*not* _emphasis_ & <tag>`.

`run` after

Merged `code`.

`bold code`

<del>`struck code`</del>

A paragraph style called Source Text is not code.

```
int main() {
   return 0;
}
```

Between the fences.

````
first

second ``` third
after a break
````

After.
