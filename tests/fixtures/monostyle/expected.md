# Chapter One

Word puts a theme slot in docDefaults and the real font on Normal, so this is the **likelier** half of the same guard.

A character style still says `code`, because that is about the run.

```
And a run that names its own monospace family is code again.
```

*A character style based on that paragraph style must not inherit its family.*
